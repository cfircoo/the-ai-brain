# {{BRAIN_NAME}} - Cursor Rules

# Auto-generated by The AI Brain. Edit brain.yaml and run `vault-align` to update.
# Last generated: {{GENERATED_DATE}}

# Owner: {{OWNER_NAME}} ({{OWNER_ROLE}})

# ─── Vault Structure ───────────────────────────────────────────────────────────
# Human-authored (READ only unless explicitly asked):
#   {{HUMAN_DIR}}/          - Human content root
#   {{DAILY_DIR}}/          - Daily notes (YYYY-MM-DD.md)
#   {{PROJECTS_DIR}}/      - Project files
#   {{ARCHIVE_DIR}}/       - Archived items
#
# AI-managed (READ and WRITE freely):
#   {{MACHINE_DIR}}/       - Machine content root
#   {{SESSION_LOGS_DIR}}/  - Session continuity logs
#   {{MEMORY_DIR}}/        - Persistent memory
#   {{RULES_DIR}}/         - Behavioral rules
#   {{TEMPLATES_DIR}}/     - Templates

# ─── Startup ───────────────────────────────────────────────────────────────────
# Before doing ANY work:
# 1. Read {{RULES_DIR}}/active-rules.md
# 2. Read {{MEMORY_DIR}}/context-cache.md
# 3. Read the most recent file in {{SESSION_LOGS_DIR}}/
# 4. Read today's daily note if it exists

# ─── Formatting ────────────────────────────────────────────────────────────────
{{FORMAT_RULES}}

# ─── Brain Rules ───────────────────────────────────────────────────────────────
{{BRAIN_RULES}}

# ─── Session End ───────────────────────────────────────────────────────────────
# Before ending a session, write a session log to {{SESSION_LOGS_DIR}}/
# using the template at {{TEMPLATES_DIR}}/session-log.md.
