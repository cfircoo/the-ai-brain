# {{BRAIN_NAME}} - AI Brain (Gemini)

> This file is auto-generated by The AI Brain. Edit brain.yaml and run `vault-align` to update.
> Last generated: {{GENERATED_DATE}}

## Owner
- **Name:** {{OWNER_NAME}}
- **Role:** {{OWNER_ROLE}}

## Vault Layout

You are operating inside a structured knowledge vault. The vault has two top-level areas with distinct permissions:

### Human Directory (`{{HUMAN_DIR}}/`) -- READ ONLY
Do not modify files here unless the user explicitly asks you to.
- `{{DAILY_DIR}}/` -- Daily notes in YYYY-MM-DD.md format
- `{{PROJECTS_DIR}}/` -- Project files and documentation
- `{{ARCHIVE_DIR}}/` -- Completed or archived items

### Machine Directory (`{{MACHINE_DIR}}/`) -- READ and WRITE
This is your workspace. Create and update files freely.
- `{{SESSION_LOGS_DIR}}/` -- Session continuity logs
- `{{MEMORY_DIR}}/` -- Persistent memory (entities, decisions, corrections)
- `{{RULES_DIR}}/` -- Self-updating behavioral rules
- `{{TEMPLATES_DIR}}/` -- Note and project templates

## Startup Instructions

Complete these steps before doing any work:

1. Read `{{RULES_DIR}}/active-rules.md` for current behavioral rules.
2. Read `{{MEMORY_DIR}}/context-cache.md` for active context.
3. Read the most recent file in `{{SESSION_LOGS_DIR}}/` for session continuity.
4. Check if today's daily note exists and read it for current priorities.

## Formatting Conventions
{{FORMAT_RULES}}

## Brain Rules
{{BRAIN_RULES}}

## Session End Protocol
Before ending a session, write a session log to `{{SESSION_LOGS_DIR}}/` using the template at `{{TEMPLATES_DIR}}/session-log.md`. Include a summary of what was done, decisions made, and next steps.
